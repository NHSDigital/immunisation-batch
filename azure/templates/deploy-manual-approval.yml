parameters:
  - name: reviewers
    type: object
    default: []
environment:
  name: 'staging'
  resourceType: 'VirtualMachine'
  approvals:
    - reviewers: ${{ parameters.reviewers }}
      timeoutInMinutes: 60